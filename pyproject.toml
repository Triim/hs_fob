[project]
name = "hs-fob"
version = "0.1.0"
description = "GradED — a blockchain protocol for reproducible, expert-attested competence certification."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.1",
    "cryptography>=49.0.0",
    "pyipv8>=3.2.1",
]
