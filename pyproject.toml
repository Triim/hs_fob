[project]
name = "hs-fob"
version = "0.1.0"
description = "GradED — a from-scratch Python blockchain for decentralized competence attestation: PoA-proposed, BFT-quorum-finalized blocks carrying signed attestations and submissions, with chain-derived reputation, token balances, endogenous validator promotion, and evidence-based quorum slashing."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.1",
    "cryptography>=49.0.0",
    "pyipv8>=3.2.1",
]
