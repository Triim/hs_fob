# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# macOS
.DS_Store

# Local chain / node state (generated at runtime)
state/
*.chain

# IPv8 keypairs — generated locally, never commit
*.pem

# IPv8 runtime working directory (wallet/metadata DB) — generated at runtime
sqlite/

# python version pin
.python-version

# Claude Code local files — machine-specific, never commit
.claude/
