# Core blockchain: standard library only (hashlib, dataclasses, time, json).
# No third-party dependencies are required to run or test the core.
#
# The networking layer is added in a later step:
#   pyipv8>=3.2.1   # peer-to-peer overlay — NOT used by the core yet
