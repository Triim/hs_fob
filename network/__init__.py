"""Networking layer (CS414).

This package carries core objects between nodes. It starts with a pure
serialization *seam* (:mod:`network.wire`) that is tested in isolation before any
actual peer-to-peer code (py-ipv8) is introduced in a later step.
"""
